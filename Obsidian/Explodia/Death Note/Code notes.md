Make opmode for debug

Try to use bare .anim file instead of animation library

Try pixel composer demo

We make combat and movement and ai then we start working on server stuff then after that we do all the data stuff(inventory , etc)